PORT=5000

# PostgreSQL Connection URL (Local or Cloud e.g. Neon.tech / Supabase / Render)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/metrx?schema=public"

# JWT Secret
JWT_SECRET=metrx_super_secure_jwt_secret_key_2025
NODE_ENV=development

# Optional Nodemailer / SMS config
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

